# Clock
A simple command-line interface (CLI) application that displays the time set by user in 12- and 24-hour format simultaneously.

## Build Guide
### Linux
```bash
# Install dependencies

# Build the program
cmake -B build
cmake --build build
```
Program binaries would be located in [{PROJECT}/build/Clock/](../build/ItemTracker).

### Windows
TBD.
